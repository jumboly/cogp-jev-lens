# JEV への到達経路は Vercel AI Gateway（docs/issues/04-jev-bff.md）。
# 鍵は Vercel のダッシュボードの AI Gateway で発行する（$5 の無料枠がある）。
#
# 無くても POI 表示・地図操作・タグ抽出は動く。AI Lens を入れたときだけ
# BFF が 500（AI_GATEWAY_API_KEY が .env にない）を返す。
AI_GATEWAY_API_KEY=
