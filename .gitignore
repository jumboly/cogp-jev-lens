# POI COGP は 2.1GiB。手元に置くが git には入れない（取得手順は README）
data/
reports/tag-profile/.duckdb-tmp/
reports/tag-profile/run.log

node_modules/
dist/
.env
.env.*
# 雛形は追跡する（鍵は入れない）
!.env.example
.DS_Store
.wrangler/
experiments/*/results/
experiments/*/fetch.log

# JEV 呼び出しの生ログ（調査用。鍵は含まないが request body の要約が入る）
logs/
